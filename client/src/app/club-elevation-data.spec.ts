import { ClubElevationData } from './club-elevation-data';

describe('ClubElevationData', () => {
  it('should create an instance', () => {
    expect(new ClubElevationData()).toBeTruthy();
  });
});
